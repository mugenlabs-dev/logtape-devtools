---
"@mugenlabs/logtape-devtools": patch
---

Pin `@base-ui-components/react` to the exact release candidate the plugin is tested against, instead of a caret range that could resolve to a newer, incompatible release candidate.
