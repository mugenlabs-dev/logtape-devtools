---
"@mugenlabs/logtape-devtools": patch
---

Render records with a log level this version does not know about (possible with newer LogTape releases) using the "info" colours and the raw level as the badge label, instead of crashing the panel.
